# Merge gate

Before this bootstrap is merged into `trajectory-lab`, require the lightweight unit workflow to pass. GPU validation is the next gate and is not required for this manifest-only scaffold, but no latent-persistence work should be treated as validated until the fixed-seed GTX 1650 Super comparison in `TESTING.md` is completed.
